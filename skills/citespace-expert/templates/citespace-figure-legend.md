# CiteSpace Figure Legend Template

**Figure X. [Analytical network and topic].** Records were retrieved from [database/collection/indexes] for [timespan] and analyzed in CiteSpace [version/build]. Nodes represent [unit], and links represent [relationship]. Node size indicates [encoding]; node color/rings indicate [temporal encoding]; [centrality or other ring] indicates [encoding]. Nodes were selected using [criterion/parameters] with [years]-year time slices. [Pruning method] was applied. The network contains [nodes] nodes and [links] links with density [value]. Interpretations are limited to the analyzed corpus.

Delete fields that do not apply. Never fill missing values by estimation.
